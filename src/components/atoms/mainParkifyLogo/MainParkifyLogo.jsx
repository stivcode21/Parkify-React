import styles from "./MainParkifyLogo.module.css";

const MainParkifyLogo = () => {
  return (
    <h1 className={styles.title}>
      Parki<span className={styles.strong}>fy</span>
    </h1>
  );
};

export default MainParkifyLogo;
