import styles from "./LogoParkify.module.css";

const LogoParkify = () => {
  return (
    <h1 class={styles.title}>
      Parki<span class={styles.strong}>fy</span>
    </h1>
  );
};

export default LogoParkify;
